# このリポジトリで作業するときの決めごと

ZCnova BGM Studio（`ZCNOVA_BGM_v5.6.html` 1枚で動く BGM 生成ツール）。

**ここに書いてあるのは、実際にやらかして直したことだけです。**
毎回同じ失敗を繰り返さないために書いてあります。

---

## ⚠️ 1. 文字コード ― 何度も文字化けさせている

**日本語の `.txt` は必ず「BOM 付き・CRLF」で書き出すこと。**

| ファイル | BOM | 改行 | なぜ |
|---|---|---|---|
| **日本語の `.txt`**（説明書・権利表記） | **付ける** | **CRLF** | BOM が無いと Windows のメモ帳や日本語エディタが **Shift-JIS だと思い込んで**開き、全部化ける。`.txt` には `<meta charset>` のような名乗る場所が無いので BOM しか手が無い。LF だけだと古いメモ帳が改行と見なさず、65行の説明書が **1行にべったり**出る |
| `.html` | 付けない | CRLF（後述） | `<meta charset="UTF-8">` で自分で名乗れる。BOM があると先頭に見えない文字が入って表示が崩れることがある |
| `robots.txt` | 付けない | LF | BOM があると巡回側が1行目を読み損なう |
| `.md` `.mjs` `.json` | 付けない | LF | 開発用。Windows のメモ帳で開くものではない |

**やり方**：`build/make-dist.mjs` の `説明書を書く()` / `説明書を写す()` を必ず通すこと。
`fs.copyFileSync` や `fs.writeFileSync` で直接書くと BOM が付かない。

**見張り**：検査⑳が、組み立てた `.txt` を全部調べて落とす。

> ヒアドキュメント（`cat > x.txt <<'EOF'`）や `Write` ツールで日本語 txt を作ると
> BOM 無し・LF になる。**必ず組み立てを通すか、通したものと差し替えること。**

---

## ⚠️ 2. 本体 HTML は CRLF ― 編集で壊さない

`ZCNOVA_BGM_v5.6.html` は **CRLF** で保存されている。

Python で編集するときは必ず `newline=''` を付ける。付けないと全行が LF に
変換され、**中身は同じなのに全行が差分**になる。

```python
s = io.open(p, encoding='utf-8', newline='').read()
# 置換文字列も CRLF に直してから当てる
s = s.replace(old.replace('\n','\r\n'), new.replace('\n','\r\n'))
io.open(p, 'w', encoding='utf-8', newline='').write(s)
```

---

## ⚠️ 3. 乱数の消費数を変えない

曲は `rnd()` の並びで決まる。**消費する回数が1回でも変わると、旋律・リズム・
和音まで全部ずれて別の曲になる。**

- `pick()` は配列の長さに関係なく `rnd()` を1回しか使わない。
  → **候補の中身を変えるのは安全**。`pick()` を1回増やすのは危険
- 後から手を入れるときは `seedNoise()` を使う（`rnd()` を消費しない）
- 曲データへ**焼き込む**こと。再生時に計算すると再生と書き出しがずれる

**見張り**：検査②（指紋）が62曲をパートごとに突き合わせる。
意図した変更なら `ZC_UPDATE=1 node tests/run.mjs` で控えを取り直すが、
**「動いたのは意図した所だけ」を証明してから**にすること。

---

## ⚠️ 4. 楽器を「完全ランダム」の候補に足すと、既存の曲が全部変わる

`INSTRUMENTS` に足すと `allPitched()` が変わり、全曲調の抽選がずれる。

> 実測：エレキギターとキーボードを足しただけで、控えの62曲のうち
> **47〜53箇所**が別の曲になった（和風にエレキギター、子守唄にロックオルガンも出た）

ジャンル専用の楽器には `noRandom: true` を付ける。編成と手動選択からは使える。

---

## ⚠️ 5. 音源スペックの表をまとめて置換しない

`ZCNOVA_ACOUSTIC_SPECS` は「楽器1つにつき14行ほどの塊」が32個縦に並んでいる。
ここを正規表現でまとめて書き換えると、**別の楽器に当たる。**

```python
# ❌ これをやって事故った
re.search(r'(\n    NAME: \{(?:.|\n)*?detuneMix:)([0-9.]+)', s)
```

`NAME:` は `INSTRUMENTS`（表の上のほう）にも同じ名前で出てくる。そちらには
`detuneMix` が無いので、`(?:.|\n)*?` が**次の楽器の塊まで走って**しまう。

> 実測：アコーディオンの 0.26 がフルートへ、ヴァイオリンの 0.17 がヴィオラへ
> 書き込まれた。音は鳴るし例外も出ないので、検査②（音符の指紋）にも掛からない

**やり方**：楽器の値を直すときは**正規表現を使わない。**
その楽器の行を丸ごと（`range:` から `outGain` まで）文字列で一致させて置き換える。
1楽器ずつ、当てるたびに `git diff` を見る。

**見張り**：検査㉑が32楽器のパラメーターを控えと突き合わせる。
狙って変えたなら `ZC_UPDATE=1 node tests/run.mjs 21` で控えを取り直すが、
**どの楽器のどの値をなぜ変えたかをコミット文に書くこと。**

---

## ⚠️ 6. `dist/` と `public/` は git の扱いが逆

| | git | なぜ |
|---|---|---|
| `dist/` の中身 | **入れない** | 毎回組み立て直せる（zip だけ残す） |
| `public/` | **入れる** | 公開するファイルそのもの。ホスティングは git にあるファイルしか見ないので、外すと**手元では動くのにサイトが空になる** |

---

## 音づくりで守ること

くわしくは `docs/心地よい音の実測メモ.md`（本体の先頭にも同じ索引がある）。

- **音量は必ず同じ高さ（G4）で測る。** A特性は周波数で変わるので、楽器ごとの
  中央音で測ると比べたことにならない（そろえたつもりで 0.8dB → 5.6dB に広げた）
- **A特性だけでは足りない。** 広帯域の音（シンセ・アコーディオン）のうるささを
  過小評価する。臨界帯域ごとの和も見る
- **音色を差し替えただけでは、その楽器の音にならない。** その楽器が実際にやる
  動き（スラー・和音の置き場所・声数）まで写して初めてその楽器に聞こえる
- **「音が割れて止まる」は音量ではなく計算量。** 毎秒いくつ部品を作っているかで見る
- **正規化のある系で「音量を下げる」は、何もしないのと同じ。**
  書き出しは必ずピーク0.89へ正規化される。効くのは歪み・伸び・他パートとの比率
- **ボタンの絵を文字で描かない。** 字体にその字が無いと、四角の中に文字番号が描かれる
- **予想の所要時間を画面に書かない。** 必ず外れる。書いてよいのは経過時間だけ

---

## 作業の進め方

```bash
npm test                    # 検査一式（142件・およそ3.5分）
node tests/run.mjs 08       # 番号の合うものだけ
npm run build               # dist/ と public/ を組み立て直す
```

Playwright / terser が全体に入っている環境では：

```bash
export ZC_PLAYWRIGHT=/path/to/playwright/index.js
export ZC_TERSER=/path/to/terser/main.js
```

### 直したら

1. **実測してから直す。** 「たぶんこれが原因」で触らない。数字を取る
2. 検査を回す（音楽が変わる直しなら②の指紋を必ず見る）
3. `npm run build` で配布物と `public/` を組み立て直す
4. 直した理由と**実測値**をコードのその場にコメントで残す
   （このプロジェクトは「なぜそうなっているか」をコードに書く流儀）
5. うまくいかなかった方法も、**なぜ駄目だったか実測つきで**残す
   （同じ罠を踏み直さないため。例：`zcScaleTrackVols` のコメント）
